<section id="about">
  <% if (config.description) { %>
    <%- markdown(config.description) %>
  <% } %>
  <br>
  <p style="">
    <% if (theme.customize.social_links) { %>
      You can find me in many places around the web, such as:
      <ul id="sociallinks">
        <% for(var i in theme.customize.social_links) { %>
          <li><a target="_blank" href="<%- url_for(theme.customize.social_links[i]) %>" title="<%= i %>"><%= i %></a>&nbsp;<a class="icon fa fa-<%= i %>" target="_blank" href="<%- url_for(theme.customize.social_links[i]) %>" title="<%= i %>">&nbsp;</a></li>
        <% } %>
         <li><a target="_blank" href="mailto:&#97;&#108;&#101;&#120;&#x61;&#110;&#x64;&#x70;&#x69;&#x6e;&#116;&#x6f;&#64;&#103;&#109;&#97;&#x69;&#108;&#x2e;&#x63;&#x6f;&#x6d;" title="email" rel="noopener">email</a>&nbsp;<a class="icon fa fa-send" target="_blank" href="mailto:&#97;&#108;&#101;&#120;&#x61;&#110;&#x64;&#x70;&#x69;&#x6e;&#116;&#x6f;&#64;&#103;&#109;&#97;&#x69;&#108;&#x2e;&#x63;&#x6f;&#x6d;" title="email" rel="noopener">&nbsp;</a></li>
      </ul>

    <% } %>
  </p>
</section>


<section id="writing">
  <span class="h1"><a href="<%- url_for("archives") %>">Blog</a></span>
  <ul class="post-list">
    <% if (theme.customize.show_all_posts) { %>
      <% var show_posts = page.posts.sort('date', 'desc') %>
    <% } else { %>
      <% var show_posts = site.posts.sort('date', 'desc').limit(theme.customize.post_count || 5) %>
    <% } %>
    <% show_posts.each(function(post, i){ %>
      <li class="post-item">
        <%- partial('_partial/post/date', { post: post, class_name: 'meta' }) %>
        <span><%- partial('_partial/post/title', { post: post, index: true, class_name: '' }) %></span>
      </li>
    <% }); %>
  </ul>
  <% if (theme.customize.show_all_posts) { %>
    <%- partial('_partial/pagination') %>
  <% } %>
</section>


<% if (site.data.projects) { %>
<section id="projects">
  <span class="h1"><a href="<%- url_for(theme.projects_url) %>">Projects</a></span>
  <ul class="project-list">
    <% for(var obj in site.data.projects){ %>
      <li class="project-item">
        <a href="<%= site.data.projects[obj].url %>"><%= site.data.projects[obj].name %></a>: <%= site.data.projects[obj].desc %>
      </li>
    <% } %>
  </ul>
</section>
<% } %>
